version: 1
applications:
  - appRoot: mokebook
    frontend:
      phases:
        preBuild:
          commands:
            # The Trick: Move super_admin files into the folder Amplify expects (mokebook)
            - cd ..
            - rm -rf mokebook/*
            - cp -R super_admin/* mokebook/
            - cd mokebook
            - echo "NEXT_PUBLIC_API_URL=https://edixo.onrender.com/api" > .env.production
            - echo "NEXT_PUBLIC_APP_TYPE=super-admin" >> .env.production
            - npm install
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
