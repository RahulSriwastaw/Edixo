version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd mokebook && npm install
    build:
      commands:
        - cd mokebook && npm run build
  artifacts:
    baseDirectory: mokebook/.next
    files:
      - '**/*'
  cache:
    paths:
      - mokebook/node_modules/**/*
