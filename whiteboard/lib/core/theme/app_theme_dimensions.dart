/// AppThemeDimensions - Compact spacing system
/// All padding reduced by ~30% from previous values
class AppThemeDimensions {
  // ── Structural ────────────────────────────────────────────────────
  static const double topBarHeight = 40.0;        // was 48
  static const double bottomBarHeight = 44.0;     // was 56
  static const double leftToolbarWidth = 56.0;
  static const double slidePanelWidth = 180.0;
  static const double stylePanelWidth = 260.0;    // was 280
  static const double toolLibraryWidth = 300.0;   // was 320

  // ── Widgets ───────────────────────────────────────────────────────
  static const double toolIconSize = 20.0;        // was 24
  static const double toolButtonSize = 40.0;      // was 44
  static const double toolButtonRadius = 6.0;     // was 8
  static const double resizeHandleSize = 10.0;

  // ── Border Radii ──────────────────────────────────────────────────
  static const double borderRadiusS = 4.0;
  static const double borderRadiusM = 6.0;        // was 8
  static const double borderRadiusL = 8.0;        // was 12
  static const double borderRadiusCard = 8.0;
  static const double borderRadiusButton = 6.0;
  static const double borderRadiusPill = 20.0;

  // ── Padding ───────────────────────────────────────────────────────
  static const double paddingXS = 4.0;
  static const double paddingS = 6.0;             // was ~8
  static const double paddingM = 8.0;             // was ~12
  static const double paddingL = 12.0;            // was ~16
  static const double paddingXL = 14.0;           // was ~20
  static const double paddingCard = 12.0;         // 12-14px card padding

  // ── Gaps ──────────────────────────────────────────────────────────
  static const double gapXS = 4.0;
  static const double gapS = 6.0;
  static const double gapM = 8.0;
  static const double gapL = 12.0;                // grid card gap
  static const double gapXL = 16.0;

  // ── Sidebar Nav ───────────────────────────────────────────────────
  static const double navItemHeight = 38.0;       // 38-40px max

  // ── Status Pills ──────────────────────────────────────────────────
  static const double pillPaddingH = 9.0;
  static const double pillPaddingV = 3.0;

  // ── Question Widget Constraints ───────────────────────────────────
  static const double qwMinWidth = 200.0;
  static const double qwMaxWidth = 1800.0;
  static const double qwMinHeight = 100.0;
  static const double qwMaxHeight = 900.0;
}
