{
  "name": "whiteboard-sync-server",
  "version": "1.0.0",
  "description": "Real-time synchronization server for EduHub Whiteboard",
  "main": "server.ts",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only server.ts",
    "start": "ts-node server.ts"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8.5.10",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0"
  }
}
